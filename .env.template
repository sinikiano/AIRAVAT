# Environment Configuration Template for AIRAVAT Supabase

# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Vercel Deployment (if using environment variables)
NEXT_PUBLIC_SUPABASE_URL=$SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY

# Optional: Custom domain configuration
CUSTOM_DOMAIN=your-custom-domain.com

# Storage Configuration
STORAGE_BUCKET=device-files
MAX_FILE_SIZE_MB=50

# Security Configuration (for production)
ENABLE_RLS=true
ENABLE_AUTH=false

# Debug Configuration
DEBUG_MODE=false
LOG_LEVEL=info

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual Supabase credentials
# 3. Add .env.local to your .gitignore file
# 4. Never commit actual credentials to version control

# Obtaining Supabase credentials:
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings → API
# 3. Copy the Project URL and API keys from there

# Security Notes:
# - Keep your service role key secret and secure
# - Only use anon key in client-side code
# - Service role key should only be used in server-side code
# - Consider using environment variables in production deployments
